# Pinned to the versions validated on the reference device — updates run
# `pip install -r` on every software update, so unpinned entries would make
# each update a dependency lottery.
flask==3.1.3
flask-cors==6.0.2
psycopg2-binary==2.9.11
Flask-SQLAlchemy==3.1.1
redis==7.2.1
pyserial==3.5
python-dotenv==1.2.1
gunicorn==25.1.0
flask-sse==1.0.0
gevent==25.9.1
flask-jwt-extended==4.7.1
bcrypt==5.0.0
